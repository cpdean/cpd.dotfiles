" the internet does not say i need to do this, but i cannot do it the way the
" internet tells me to anyway so going to just boostrap this in the init.vim
"lua require("init")
"
" switch back to old vim

source $HOME/.config/nvim/backup.init.vim
