-- core editor config (no plugins). loaded before the backup.init.vim shim.
-- modules get added here as phase 1 extracts them: options, autocmds,
-- providers, keymaps.
require('cpdean_config.core.options')
