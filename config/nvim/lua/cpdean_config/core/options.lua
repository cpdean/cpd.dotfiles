-- editor options, migrated from backup.init.vim (gradual-refactor phase 1).
-- starting with one option to establish the pattern; the rest of the
-- set-options follow in a later bulk commit.

vim.opt.number = true
